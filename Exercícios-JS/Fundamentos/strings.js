/** STRINGS são cadeias de caracteres delimitadas por aspas simples, duplas ou crases (template string) */

const escola = "Cod3r";

console.log(escola.charAt(4)); // Retorna o caracter de índice 4 que, no caso, é a letra r.
/** OU */
console.log(escola[4]); // Retorna o  mesmo que o exemplo anterior.

